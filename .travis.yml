os: osx
osx_image: xcode7.3

install:
  - brew install autoconf213 libidl llvm node rust ccache cmake gnu-tar python nasm yasm watchman
  - cargo install sccache

script:
- ./mach build
- ./mach package
