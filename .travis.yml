os: osx
osx_image: xcode8

addons:
  homebrew:
    packages:
    - autoconf213

install:
  - brew install libidl llvm@10 node sccache ccache cmake gnu-tar python nasm yasm watchman

script:
- ./mach build
- ./mach package
