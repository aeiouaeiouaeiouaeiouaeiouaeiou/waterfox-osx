os: osx
osx_image: xcode8

addons:
  homebrew:
    packages:
    - libidl
    - llvm@10
    - node
    - sccache
    - mercurial
    - nasm
    - yasm
    - watchman

install:
  - brew install autoconf@2.13 ccache cmake gnu-tar python

script:
- ./mach build
- ./mach package
